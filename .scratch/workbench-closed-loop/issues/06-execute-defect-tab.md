# 06 — 执行 Tab + 缺陷 Tab + 一键提缺陷

**What to build:** 用户可在工作台内执行绑定的自动化用例，失败结果旁一键提缺陷。缺陷 Tab 展示关联需求的缺陷。

**Blocked by:** 05 — 测试点 + 场景 + 用例全链路

**Status:** pending

- [ ] 新增 `POST /api/requirements/{req_id}/execute` 端点（接收 uids[] → 创建 Task + Execution → 返回 execution_id）
- [ ] 执行 Tab：展示当前需求绑定的自动化用例列表（勾选/全选/[▶ 执行选中] 按钮）
- [ ] 执行进度实时展示（WebSocket 推送，进度条 + 各用例状态图标）
- [ ] 执行结果列表：pass/fail 行，每行显示用例名/耗时/状态。失败行高亮红色
- [ ] 失败结果旁 [🐛 提缺陷] 按钮 → 弹窗自动预填：标题（含用例名+失败摘要）、关联需求 ID、关联用例 UID、步骤（从 GeneratedCase 自动填入）、日志（从执行结果自动填入）
- [ ] 缺陷 Tab：展示关联当前需求的缺陷列表（标题/严重程度/状态/指派给/创建时间）
- [ ] 点击缺陷行展开详情：描述 + 活动记录时间线，不跳页
- [ ] [在缺陷管理页打开] 链接：跳转到 `/defects` 全局缺陷管理页
- [ ] 手动新建缺陷按钮：弹窗预填关联需求 ID
