# 02 — 工作台框架：左右分栏 + 4 Tab

**What to build:** 需求列表和需求详情合并为一个左右分栏页面。右侧 4 个 Tab（概览/资产/执行/缺陷）。新增 `GET /workbench` 聚合端点。

**Blocked by:** 01 — DB 新 Schema + 后端模块拆分

**Status:** pending

- [ ] 新增 `GET /api/requirements/{req_id}/workbench` 聚合端点（返回需求详情 + 概览统计 + 空分层资产列表）
- [ ] `/requirements` 路由改为左右分栏布局：左侧 320px 需求列表 + 右侧详情面板
- [ ] 需求列表支持搜索（标题模糊匹配）和状态筛选（全部/待评审/评审通过/Story确认/用例生成/已完成）
- [ ] 右侧 4 个 Tab 骨架：概览、资产、执行、缺陷（每个显示占位内容）
- [ ] 点击左侧需求名称切换右侧选中需求，URL 更新为 `?req_id=X`
- [ ] 分支切换时刷新左侧列表，若选中需求不在新分支则清空右侧面板
