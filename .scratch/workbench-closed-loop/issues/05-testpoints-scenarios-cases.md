# 05 — 测试点 + 场景 + 用例全链路

**What to build:** Story 确认后 AI 逐层生成测试点树、测试场景、测试用例，用户可逐层确认。用例可绑定自动化用例。

**Blocked by:** 04 — Story 生成 + 评审 + 资产 Tab

**Status:** pending

- [ ] 资产 Tab 内测试点视图：树形结构（11 类分类 + parent_id 层级）
- [ ] AI 生成测试点 → 测试点评审（11 维）→ 人工确认 → 进入场景层
- [ ] 资产 Tab 内场景视图：按测试点组织，每个场景标注覆盖维度（正常/异常/边界/状态）
- [ ] AI 场景评审 → 人工确认 → 进入用例层
- [ ] 资产 Tab 内用例视图：列表展示，每条包含标题/前置条件/步骤/预期结果/评分
- [ ] AI 用例评审（9 维检查）→ 人工确认
- [ ] 用例绑定自动化用例：弹窗搜索 TestCaseDefinition → 多选绑定 → 展示已绑定列表
- [ ] 资产 Tab 内层级导航：Story → 测试点 → 场景 → 用例，面包屑可回退
